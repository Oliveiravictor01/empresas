rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write during development and multi-device connection
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
